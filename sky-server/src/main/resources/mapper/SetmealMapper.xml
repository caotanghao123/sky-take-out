<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal (category_id,
                             name,
                             price,
                             status,
                             description,
                             image,
                             create_time,
                             update_time,
                             create_user,
                             update_user)
        values (#{categoryId},
                #{name},
                #{price},
                #{status},
                #{description},
                #{image},
                #{createTime},
                #{updateTime},
                #{createUser},
                #{updateUser})
    </insert>
    <select id="getBySetMealId" resultType="com.sky.vo.SetmealVO">
        select setmeal.*, category.name as category_name
        from setmeal
        left join category on setmeal.category_id = category.id
        <where>
            <if test="id != null">
                and setmeal.id = #{id}
            </if>
        </where>
    </select>
</mapper>
